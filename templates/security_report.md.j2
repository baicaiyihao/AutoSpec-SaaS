# Security Audit Report

## Overview

| Field | Value |
|-------|-------|
| **Module** | `{{ module_name }}` |
| **Domain** | {{ domain_tag }} |
| **Risk Score** | {{ risk_score }}/100 |
| **Scan Time** | {{ timestamp }} |
| **Issues Found** | {{ total_issues }} |

## Severity Distribution

{% for sev, count in severity_dist.items() %}
- {{ severity_emoji[sev] }} **{{ sev|upper }}**: {{ count }}
{% endfor %}

---

## Findings

{% for finding in findings %}
### {{ loop.index }}. {{ finding.severity_emoji }} [{{ finding.severity|upper }}] {{ finding.title }}

| Field | Value |
|-------|-------|
| **ID** | `{{ finding.pattern_id }}` |
| **Confidence** | {{ finding.confidence }}% |
| **Tags** | {{ finding.tags | join(', ') if finding.tags else 'N/A' }} |

#### Location

- **Module**: `{{ finding.module }}`
- **Function**: `{{ finding.function or 'N/A' }}`
- **Line**: {{ finding.line or 'N/A' }}

#### Code Context

```move
{{ finding.code_context }}
```

#### Recommendation

{{ finding.recommendation }}

{% if finding.suggested_checks %}
#### Suggested Spec Checks

{% for check in finding.suggested_checks %}
- `{{ check }}`
{% endfor %}
{% endif %}

---

{% endfor %}

## Disclaimer

> Formal verification passing does NOT guarantee security.
> Please manually review all findings above.

---

*Generated by AutoSpec Security Scanner*
