# AutoSpec - AI 驱动的智能合约自动化审计平台

<div align="center">

![Version](https://img.shields.io/badge/version-3.3.1-blue)
![Python](https://img.shields.io/badge/python-3.9+-green)
![License](https://img.shields.io/badge/license-MIT-orange)

**AutoSpec** 是一个基于多代理 AI 系统的智能合约安全审计平台，专注于 Sui Move 智能合约的漏洞检测与分析。

[快速开始](#-快速开始) • [功能特性](#-核心功能) • [架构设计](#-系统架构) • [文档](#-文档)

</div>

---

## 📖 项目简介

AutoSpec 是一个现代化的智能合约安全审计平台，提供全自动化的漏洞检测与分析能力：

- **智能安全审计**：多代理 AI 系统进行深度漏洞扫描与验证
- **Web3 SaaS 模式**：Sui 钱包登录 + 链上 Token 充值，去中心化身份与支付
- **Web UI**：现代化的用户界面，支持项目管理、审计报告、用户权限控制
- **规则引擎**：27+ 内置排除规则 + 可自定义规则库
- **双计费模式**：支持自有 API Key / 平台 Token 两种付费方式

## ✨ 核心功能

### 🔍 智能安全审计

#### 三阶段审计流程
```
Phase 1: 项目分析
    ↓ 代码解析、依赖分析、调用图构建
Phase 2: 漏洞扫描 (Auditor Agent)
    ↓ 模式匹配、语义分析、风险评估
Phase 3: 漏洞验证 (Verifier Agent)
    ↓ 多视角验证（安全审计 + Move 专家 + 业务分析）
Phase 4: 攻击链分析 (WhiteHat Agent - 仅 HIGH/CRITICAL)
    ↓ PoC 生成、利用路径分析
```

#### 核心能力
- **两层扫描机制**：规则模式扫描 + AI 语义分析
- **误报过滤**：27+ Sui Move 语言特性排除规则
- **多视角验证**：统一 Verifier Agent 整合 3 种专家视角
  - Security Auditor: 已知漏洞模式、安全最佳实践
  - Move Expert: Move 类型系统、Sui 对象模型保护
  - Business Analyst: 攻击经济学、真实场景可行性
- **攻击链分析**：入口点 → 攻击路径 → 影响分析 → PoC 生成

### 🌐 现代化 Web UI (v3.3.1)

#### 项目管理
- 创建项目（上传代码/导入本地路径）
- 项目配置（模型预设、代理架构、安全扫描开关）
- 批量管理、搜索过滤

#### 审计执行
- 一键启动审计
- 实时进度追踪
- WebSocket 日志流
- 审计历史记录

#### 报告系统
- 按严重级别分类（CRITICAL/HIGH/MEDIUM/LOW/ADVISORY）
- 代码位置定位（Monaco Editor 集成）
- AI Review 对话（误报复审、安全建议）
- Markdown/JSON 导出

#### 用户管理
- **双登录模式**：
  - C端用户：Sui 钱包签名登录（去中心化身份）
  - 管理员：传统账号密码登录
- 角色权限（Admin/User）
- Token 配额管理（无限/限制）
- 注册审批流程（open/review/closed）
- 用户激活/禁用

#### Token 统计与支付
- **双计费模式**：
  - `own_key` 模式：用户配置自己的 API Key，直接向 LLM 供应商付费
  - `platform_token` 模式：使用平台共享 Key，从购买的 Token 余额扣费
- **链上充值**：使用 SUI 通过智能合约购买 Token
  - Pyth Network 实时价格预言机
  - 链上支付记录可追溯
- 用量趋势图表（按天/周/月）
- 项目用量排行
- Agent 用量分布
- 分开统计：自有 API Key 消耗 vs 平台 Token 消耗
- 实时余额扣费与不足预警

#### 规则管理
- 系统内置规则查看
- 自定义排除规则
- 规则启用/禁用
- 规则优先级管理

### 🔐 安全特性 (v3.3.1)

- **双登录模式**：
  - C端用户：Sui 钱包 Ed25519 签名验证
  - 管理员：JWT 认证 + 密码加密存储
- **JWT Refresh Token**：15 分钟访问令牌 + 7 天刷新令牌
- **图形验证码**：防暴力破解（后台可配置启用/禁用）
- **密码强度验证**：8+ 字符、大小写字母 + 数字
- **首次登录强制修改密码**：默认管理员账号安全增强
- **Token 额度管理**：
  - 审计前权限检查（API Key 或余额）
  - 实时扣费与余额不足预警
  - 审计自动终止（余额不足时）
- **数据库迁移**：Alembic 自动升级数据库结构

## 🚀 快速开始

> 💡 **新手推荐**：查看 [快速上手指南](GETTING_STARTED.md)，包含详细的配置说明和 Token 使用指南。

### 方式一：一键部署脚本

#### Linux / macOS
```bash
# 1. 克隆项目
git clone https://github.com/yourusername/AutoSpec.git
cd AutoSpec

# 2. 运行部署脚本
chmod +x setup.sh
./setup.sh

# 3. 启动后端（终端 1）
python3 scripts/start_backend.py

# 4. 启动前端（终端 2）
cd frontend && npm run dev
```

#### Windows
```cmd
# 1. 克隆项目
git clone https://github.com/yourusername/AutoSpec.git
cd AutoSpec

# 2. 运行部署脚本
setup.bat

# 3. 启动后端（终端 1）
python scripts\start_backend.py

# 4. 启动前端（终端 2）
cd frontend
npm run dev
```

### 方式二：手动安装

#### 前置要求
- Python 3.9+
- Node.js 18+
- Alibaba DashScope API Key（必需，用于 AI 审计）

#### 后端部署
```bash
# 1. 安装 Python 依赖
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，配置 API Keys：
# DASHSCOPE_API_KEY=your_key_here  # 必需
# ANTHROPIC_API_KEY=...           # 可选
# OPENAI_API_KEY=...              # 可选
# DEEPSEEK_API_KEY=...            # 可选
# ZHIPU_API_KEY=...               # 可选

# 3. 初始化数据库
python scripts/migrate.py

# 4. 启动后端
uvicorn src.api.main:app --host 0.0.0.0 --port 8000 --reload
```

#### 前端部署
```bash
cd frontend

# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm run dev

# 3. 构建生产版本（可选）
npm run build
```

### 访问应用

- **C端用户登录**: http://localhost:5173/login
  - 使用 Sui 钱包（Sui Wallet、Suiet 等）签名登录
  - 自动注册新用户
- **管理员登录**: http://localhost:5173/admin-login
  - 用户名: `admin`
  - 密码: `admin123`
  - **首次登录需强制修改密码**
- **API 文档**: http://localhost:8000/docs

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                   Web UI (React + TypeScript)               │
│  项目管理 | 审计监控 | 报告查看 | 用户管理 | 规则管理        │
└────────────────────────┬────────────────────────────────────┘
                         │ REST API + WebSocket
┌────────────────────────┴────────────────────────────────────┐
│                    FastAPI Backend                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Auth & User │  │   Project    │  │    Audit     │      │
│  │  Management  │  │  Management  │  │   Service    │      │
│  └──────────────┘  └──────────────┘  └──────┬───────┘      │
└─────────────────────────────────────────────┼──────────────┘
                                              │
┌─────────────────────────────────────────────┼──────────────┐
│              Multi-Agent Audit Engine       │              │
│  ┌──────────────────────────────────────────┼─────────┐    │
│  │ Phase 1: 代码分析                        │         │    │
│  │  ┌─────────────┐  ┌─────────────┐       │         │    │
│  │  │  CallGraph  │  │ Dependency  │       │         │    │
│  │  │   Builder   │  │  Resolver   │       │         │    │
│  │  └─────────────┘  └─────────────┘       │         │    │
│  │         │                                 │         │    │
│  │ Phase 2: 漏洞扫描                        │         │    │
│  │  ┌──────▼──────┐                         │         │    │
│  │  │  Auditor    │ 模式扫描 + 语义分析     │         │    │
│  │  └──────┬──────┘                         │         │    │
│  │         │                                 │         │    │
│  │ Phase 3: 漏洞验证                        │         │    │
│  │  ┌──────▼──────┐                         │         │    │
│  │  │  Verifier   │ 3 视角验证（统一）      │         │    │
│  │  │  (3-in-1)   │ • Security Auditor     │         │    │
│  │  │             │ • Move Expert          │         │    │
│  │  │             │ • Business Analyst     │         │    │
│  │  └──────┬──────┘                         │         │    │
│  │         │ (confidence < 80%)             │         │    │
│  │  ┌──────▼──────┐                         │         │    │
│  │  │   Manager   │ 可选复审               │         │    │
│  │  └──────┬──────┘                         │         │    │
│  │         │                                 │         │    │
│  │ Phase 4: 攻击链分析 (HIGH/CRITICAL)     │         │    │
│  │  ┌──────▼──────┐                         │         │    │
│  │  │  WhiteHat   │ PoC 生成与验证         │         │    │
│  │  └─────────────┘                         │         │    │
│  └──────────────────────────────────────────┘         │    │
└────────────────────────────────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────────┐
│                   Knowledge & Rules                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Exclusion    │  │  Security    │  │   Pattern    │      │
│  │   Rules      │  │  Knowledge   │  │   Database   │      │
│  │ (27+ rules)  │  │    Base      │  │   (JSONL)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 核心模块

#### 安全审计 (`src/security/`)
- `pattern_scan.py`: SecurityScanner（规则引擎 + 模式匹配）
- `exploit_analyzer.py`: 攻击链分析（入口 → 路径 → 影响）
- `exclusion_rules.py`: 27+ 规则自动过滤 Sui Move 误报

#### 多代理系统 (`src/agents/`)
- `engine.py`: 代理编排（架构选择）
- `auditor_agent.py`: Phase 2 漏洞扫描
- `verifier_agent.py`: Phase 3 统一 3 视角验证器
- `white_hat_agent.py`: Phase 4 漏洞利用验证（仅 HIGH/CRITICAL）
- `manager_agent.py`: 可选复审（confidence < 80%）

#### 代码理解 (`src/context/`)
- `callgraph.py`: 函数调用图构建
- `dependency_resolver.py`: 外部依赖解析
- `project_indexer.py`: 项目代码索引

#### API 后端 (`src/api/`)
- `routers/auth.py`: 认证路由（登录/注册/刷新）
- `routers/projects.py`: 项目管理
- `routers/audits.py`: 审计执行
- `routers/users.py`: 用户管理
- `routers/settings.py`: 系统配置
- `services/audit_service.py`: 审计业务逻辑

## 📊 使用场景

### CLI 模式（适合脚本/CI/CD）
```bash
# 单文件审计
python -m src.cli.audit --file path/to/contract.move

# 项目级审计
python -m src.cli.audit --project path/to/move-project

# 指定输出目录
python -m src.cli.audit --project ./my-contract --output ./reports
```

### Web UI 模式（推荐）

#### C端用户流程
1. **钱包登录**
   - 连接 Sui 钱包（Sui Wallet、Suiet 等）
   - 签名验证（自动注册新用户）
2. **充值 Token**（platform_token 模式）
   - 输入购买数量
   - 使用 SUI 链上支付
   - 实时汇率转换（Pyth Oracle）
3. **配置付费模式**
   - 选择 `own_key` 或 `platform_token`
   - `own_key`：配置自己的 API Key（在「API Keys」标签）
   - `platform_token`：使用购买的 Token 余额
4. **创建项目**
   - 上传 Move 源码文件
   - 或导入本地项目路径
5. **启动审计**
   - 系统自动检查权限（API Key 或余额）
   - 实时进度追踪和日志
   - 根据付费模式扣费或统计
6. **审查报告**
   - 代码位置跳转
   - AI Review 对话
   - 标记误报/确认
7. **导出报告**（Markdown/JSON）

#### 管理员流程
1. **密码登录**（首次需修改密码）
2. **用户管理**：审批/禁用用户、配置 Token 配额
3. **系统配置**：验证码开关、注册模式、模型预设
4. **规则管理**：启用/禁用排除规则、查看触发统计

### Python API（适合集成）
```python
from src.agents import SecurityAuditEngine, AuditConfig

# 配置审计
config = AuditConfig(
    use_simplified_architecture=True,  # 使用 3-Agent 架构（68% Token 节省）
    pattern_scan=True,                 # 启用规则扫描
    generate_exploits=True,            # 生成 PoC（仅 HIGH/CRITICAL）
)

# 运行审计
engine = SecurityAuditEngine(config)
result = await engine.run_audit(
    source_code=source_code,
    module_name="my_module",
)

# 输出结果
print(f"发现 {len(result.findings)} 个漏洞")
for finding in result.findings:
    print(f"- [{finding.severity}] {finding.title}")
```

## 📁 项目结构

```
AutoSpec/
├── src/
│   ├── api/                    # FastAPI 后端
│   │   ├── routers/            # API 路由
│   │   │   ├── auth.py         # 认证（登录/注册/刷新）
│   │   │   ├── projects.py     # 项目管理
│   │   │   ├── audits.py       # 审计执行
│   │   │   ├── users.py        # 用户管理
│   │   │   └── settings.py     # 系统配置
│   │   ├── auth/               # 认证系统
│   │   │   ├── jwt.py          # JWT 生成/验证
│   │   │   ├── captcha.py      # 图形验证码
│   │   │   └── password_validator.py  # 密码强度
│   │   └── services/           # 业务逻辑
│   │       ├── audit_service.py       # 审计服务
│   │       └── token_stats_service.py # Token 统计
│   ├── agents/                 # 多代理系统
│   │   ├── engine.py           # 代理编排
│   │   ├── auditor_agent.py    # 漏洞扫描
│   │   ├── verifier_agent.py   # 漏洞验证
│   │   ├── white_hat_agent.py  # 攻击链分析
│   │   └── manager_agent.py    # 复审管理
│   ├── security/               # 安全审计
│   │   ├── pattern_scan.py     # 规则引擎
│   │   ├── exploit_analyzer.py # 攻击链分析
│   │   └── exclusion_rules.py  # 排除规则
│   ├── context/                # 代码理解
│   │   ├── callgraph.py        # 调用图
│   │   ├── dependency_resolver.py  # 依赖解析
│   │   └── project_indexer.py  # 代码索引
│   ├── prompts/                # 提示词模板
│   ├── storage/                # 数据库模型
│   └── config.py               # 配置管理
├── frontend/                   # React 前端
│   ├── src/
│   │   ├── pages/              # 页面组件
│   │   │   ├── LoginNew.tsx    # C端钱包登录
│   │   │   ├── Login.tsx       # 管理员密码登录
│   │   │   ├── TokenPurchase.tsx  # Token 充值页面
│   │   │   ├── Dashboard.tsx   # 仪表盘
│   │   │   ├── Projects.tsx    # 项目列表
│   │   │   ├── ProjectDetail.tsx  # 项目详情
│   │   │   ├── AuditExecution.tsx # 审计监控
│   │   │   ├── ReportOverview.tsx # 报告查看
│   │   │   ├── UserSettings.tsx   # 用户设置（付费模式、Token统计）
│   │   │   ├── AdminSettings.tsx  # 系统设置
│   │   │   ├── AdminUsers.tsx     # 用户管理
│   │   │   └── AdminRules.tsx     # 规则管理
│   │   ├── components/         # 通用组件
│   │   │   ├── CodeViewer.tsx  # Monaco Editor
│   │   │   └── ErrorBoundary.tsx  # 错误边界
│   │   ├── services/           # API 调用
│   │   │   └── api.ts          # Axios 封装
│   │   └── contexts/           # 全局状态
│   │       └── AuthContext.tsx # 认证上下文
│   └── package.json
├── scripts/
│   ├── migrate.py              # 数据库迁移工具
│   └── start_backend.py        # 后端启动脚本
├── alembic/                    # 数据库迁移文件
│   └── versions/               # 迁移版本
├── data/                       # 数据目录
│   ├── autospec.db             # SQLite 数据库
│   └── knowledge_base/         # 安全知识库
├── reports/                    # 审计报告输出
│   └── security_audits/
├── setup.sh                    # Linux/macOS 部署脚本
├── setup.bat                   # Windows 部署脚本
└── requirements.txt            # Python 依赖
```

## 📚 文档

- **[快速上手指南](GETTING_STARTED.md)** - 从零开始配置和使用（推荐新手阅读）
- [Web UI 架构文档](docs/PROJECT_WEBUI.md) - v3.3 完整功能说明
- [审计系统技术文档](docs/PROJECT_AUDIT_V2.md) - 多代理架构详解
- [v3.3 更新日志](CHANGELOG_v3.3.md) - 最新功能和优化
- [Claude 开发指南](CLAUDE.md) - 项目架构与开发规范

## 🔧 配置说明

### 环境变量 (.env)

```bash
# 数据库配置（默认 SQLite）
DATABASE_URL=sqlite:///data/autospec.db

# 核心 API Key（必需）
DASHSCOPE_API_KEY=your_key_here

# 可选 API Keys（多厂商支持）
ANTHROPIC_API_KEY=...
OPENAI_API_KEY=...
DEEPSEEK_API_KEY=...
ZHIPU_API_KEY=...

# JWT 配置
JWT_SECRET_KEY=auto_generated_secret
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# 服务配置
API_HOST=0.0.0.0
API_PORT=8000
```

### 模型预设

系统支持多种 LLM 提供商组合：

```python
from src.config import get_agent_configs

# 自动检测可用 API Keys
configs = get_agent_configs("auto")

# 中国区预设（DashScope + ZhipuAI）
configs = get_agent_configs("china")

# Claude 全家桶
configs = get_agent_configs("claude")

# 混合模式（DeepSeek + Claude）
configs = get_agent_configs("hybrid")
```

## 🎯 核心优势

### vs 传统安全审计
- ✅ **自动化**：无需人工逐行审查，一键启动审计
- ✅ **一致性**：不受审计员个人经验和状态影响
- ✅ **可扩展**：规则库和知识库持续积累
- ✅ **误报控制**：27+ 规则 + AI 复审双重过滤

### vs 纯 LLM 审计
- ✅ **结构化分析**：调用图 + 依赖关系 + 语义理解
- ✅ **多视角验证**：3 种专家视角交叉验证
- ✅ **规则引擎**：预定义模式快速筛查
- ✅ **可验证性**：PoC 生成与测试（HIGH/CRITICAL）

### Token 优化 (v2.5.3+)
- **3-Agent 架构**：相比 5-Agent 可减少约 68% Token 消耗
- **WhiteHat 过滤**：仅处理 HIGH/CRITICAL 漏洞，跳过低风险项
- **知识库前置**：27+ 排除规则不消耗 LLM Token
- **实际消耗**：取决于代码复杂度和漏洞数量，建议审计后查看统计

### 排除规则示例

27+ 内置规则自动过滤 Sui Move 语言特性导致的误报：

| 规则 | 说明 | 示例 |
|------|------|------|
| Rule 1-6 | Move 语言保护 | 溢出自动 abort、类型安全、所有权模型 |
| Rule 6b | 重入攻击免疫 | Move 无动态调度 |
| Rule 7-12 | Sui Move 模式 | Capability ACL、public(package)、Shared 对象 |
| Rule 13-17 | 非安全问题 | 常量、Mock 函数、Getter |
| Rule 18-26 | 生产模式 | Cetus CLMM 实际案例分析 |

## 🐛 已知限制

- 复杂合约可能需要更长的审计时间
- 跨模块依赖需要完整的项目上下文
- 图形验证码为内存存储（生产环境建议 Redis）
- 目前仅支持 Sui Move（其他区块链语言待支持）

## 🗺️ 路线图

### v3.4（规划中）
- [ ] Docker 一键部署
- [ ] 审计报告对比（版本差异）
- [ ] 批量审计（多项目并行）
- [ ] 自定义规则编辑器（Web UI）

### v3.5（规划中）
- [ ] 团队协作功能（评论、标签、分配）
- [ ] 审计 Benchmark（准确率/召回率统计）
- [ ] 支持 Aptos Move
- [ ] 支持 Solidity

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

### 开发环境搭建
```bash
# 1. Fork 项目
# 2. 克隆到本地
git clone https://github.com/your-username/AutoSpec.git

# 3. 安装依赖
./setup.sh  # 或 setup.bat

# 4. 创建功能分支
git checkout -b feature/your-feature

# 5. 提交代码
git commit -m "feat: add your feature"
git push origin feature/your-feature

# 6. 创建 Pull Request
```

## 📄 许可证

MIT License

---

<div align="center">
Made with ❤️ by AutoSpec Team
</div>
